---
- name: "Python dev | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.apt:
    name:
      - python3-coverage
      - python3-dev
      - python3-poetry
    state: present
  become: true
  when: can_install_packages | default(false)
