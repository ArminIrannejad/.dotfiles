---
- name: "Nodejs | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.apt:
    name:
      - nodejs
      - npm
    state: present
    update_cache: true
  become: true
  when: can_install_packages | default(false)
