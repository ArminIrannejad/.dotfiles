---
- name: "Cmake | {{ ansible_facts['distribution'] }} | Install packages"
  community.general.pacman:
    name: cmake
    state: present
  become: true
  when: can_install_packages | default(false)
