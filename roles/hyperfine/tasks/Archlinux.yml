---
- name: "Hyperfine | {{ ansible_facts['distribution'] }} | Install packages"
  community.general.pacman:
    name: hyperfine
    state: present
  become: true
  when: can_install_packages | default(false)
