---
- name: "Haskell | Archlinux | Install GHCup dependencies"
  community.general.pacman:
    name:
      - gcc
      - gmp
      - libffi
      - make
      - ncurses
      - perl
      - xz
    state: present
  become: true
  when: can_install_packages | default(false)
