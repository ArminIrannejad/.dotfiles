---
- name: "Gitleaks | {{ ansible_facts['distribution'] }} | Install packages"
  community.general.pacman:
    name: gitleaks
    state: present
  become: true
  when: can_install_packages | default(false)
