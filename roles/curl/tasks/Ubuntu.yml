---
- name: "Curl | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.apt:
    name: curl
    state: present
    update_cache: true
  become: true
  when: can_install_packages | default(false)
