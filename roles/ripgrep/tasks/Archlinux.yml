---
- name: "Ripgrep | {{ ansible_facts['distribution'] }} | Install packages"
  community.general.pacman:
    name: ripgrep
    state: present
  become: true
  when: can_install_packages | default(false)
