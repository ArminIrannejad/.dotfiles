---
- name: "Clang | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.dnf:
    name:
      - clang
      - clang-devel
    state: present
  become: true
  when: can_install_packages | default(false)
