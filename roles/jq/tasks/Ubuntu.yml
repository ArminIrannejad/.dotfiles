---
- name: "Jq | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.apt:
    name: jq
    state: present
    update_cache: true
  become: true
  when: can_install_packages | default(false)
