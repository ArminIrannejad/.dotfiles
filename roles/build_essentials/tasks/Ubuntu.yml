---
- name: "Build essentials | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.apt:
    name:
      - build-essential
      - libtool
    state: present
    update_cache: true
  become: true
  when: can_install_packages | default(false)
