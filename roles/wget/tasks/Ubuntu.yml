---
- name: "Wget | {{ ansible_facts['distribution'] }} | Install packages"
  ansible.builtin.apt:
    name: wget
    state: present
    update_cache: true
  become: true
  when: can_install_packages | default(false)
